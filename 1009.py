#nome = input("Insira o nome: ")
#salario_fixo = round(float(input("Digite o salário fixo: ")), 2)
#total_vendas = round(float(input("Digite o total de vendas: ")), 2)
#comissao = 0.15 * total_vendas
#total = salario_fixo + comissao

#print("TOTAL = R$ {:.2f}".format(total))

#nome = input("primeiro nome do vendedor: ")
#salario_fixo = round(float(input()), 2)
#total_vendas = round(float(input()), 2)
#comissao = 0.15 * total_vendas
#total = salario_fixo + comissao

#print("TOTAL = R$ {:.2f}".format(total))

# -*- coding: utf-8 -*-

nome = input("") #há um erro na plataforma, pois a mesma aceita sem a aspas
salario_fixo = round(float(input()), 2)
total_vendas = round(float(input()), 2)
comissao = 0.15 * total_vendas
total = salario_fixo + comissao
print("TOTAL = R$ {:.2f}".format(total))
